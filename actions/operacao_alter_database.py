"""
    Módulo responsável por realizar as operações de validação e execução com o ALTER DATABASE.

    Sintaxe para criação da database:

    ALTER DATABASE nome_base_dados;
"""


def main_alter_database(lista_query):
    """
        Função que vai receber de fora do módulo a query em forma de lista.
        A lista será validada por ________().
        Se a query estiver ok, será encaminhada para o ________().
    """
    print("main_alter_database")
    print(lista_query)
